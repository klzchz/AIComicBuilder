# AI Comic Builder — Python runtime (port of the Next.js stack)
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.9
sqlalchemy>=2.0
python-dotenv>=1.0
httpx>=0.27
pydantic>=2.9

# AI providers (parity with @ai-sdk/openai, @ai-sdk/google, openai, @google/genai)
openai>=1.54
google-genai>=0.3

# Document import (parity with mammoth / unpdf / pdfjs-dist)
python-docx>=1.1
pypdf>=5.0

# Video processing (parity with fluent-ffmpeg — calls system ffmpeg)
ffmpeg-python>=0.2
