# Local GPU image generation (optional feature). Requires an NVIDIA GPU + CUDA.
#
# 1) Install torch matching your CUDA build first (see https://pytorch.org).
#    Example for CUDA 12.8 (e.g. RTX 40/50 series):
#       pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# 2) Then install the rest:
#       pip install -r local_sd/requirements-gpu.txt
diffusers>=0.39
transformers
accelerate
safetensors
fastapi
uvicorn
